// Zmienne wstrzykiwane przez Docker w czasie startu kontenera
// Nie edytuj tego pliku ręcznie — ustaw zmienne w Portainerze / docker-compose
window.PSTRYK_CONFIG = {
  apiKey:    "${PSTRYK_API_KEY}",
  meterIp:   "${PSTRYK_METER_IP}",
  meterPort: "${PSTRYK_METER_PORT}",
};
